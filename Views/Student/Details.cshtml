@model SchoolProject.Models.Entities.Student

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

@* <p>
    <a class="btn btn-warning" href="/Students/Enroll/@Model.Id">
        Enroll inert Course
    </a>
</p>
<h3> Enrolled Course</h3>
@if(Model.Enrollment.Count == 0)
{
    <p>
        No course Enrolled yet
    </p>
}
else
{
    <ul>
        @foreach(var e in Model.Enrollment)
        {
            <li>
                @e.Course.Title
                @e.EnrolledOn.ToShortDateString();
            </li>
        }
    </ul> *@
}

<div>
    <h4>Student</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.FullName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.FullName)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Enrollment)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Enrollment)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.Id">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
